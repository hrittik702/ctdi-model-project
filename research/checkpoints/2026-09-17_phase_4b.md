# Research Checkpoint — 2026-09-17 (Phase 4B Verified)

## Session Status
STATUS: **`PHASE_4B_EXPERIMENTAL_DATASET = COMPLETE & AUDIT-VERIFIED`**  
*(Phase 4B: Experimental Dataset Construction & Masking Protocol successfully completed and verified against experimental audit. Feature normalization statistics computed strictly on training data [294,528 station-hours, 2019-01-01 to 2021-02-05]. Chronological train/val/test splits verified with reconciled 24-hour purge buffers [24h calendar day, 25.0h physical gap, 752 excluded sliding windows]. Precision block trimming implemented [achieved block rates match targets within +-0.07%]. Multi-station outage scenarios S1, S2, S4, and S_full generated with deterministic rotating station selection. Deterministic benchmark evaluation masks generated for all 62,224 test windows. Natural NaNs [55,876 items] conserved bit-for-bit with zero target contamination. Target firewall M_natural = M_observed + M_target strictly enforced. Quality control test suite passed [17/17 tests across project in 8.70s]. 100% cryptographic raw data immutability confirmed across all 683 files. Zero models trained, zero diffusion reverse loops implemented).*

---

## 1. Objectives of Phase 4B
1. Formulate continuous feature normalization strategies calculated strictly on training observations without validation or test data leakage.
2. Establish chronological train/validation/test partitions with 24-hour temporal purge buffers to eliminate autoregressive sequence overlap.
3. Build a deterministic controlled missingness masking engine implementing Point MCAR, Continuous Temporal Block MAR, and Spatial Station Outages (S1, S2, S4, S_full).
4. Structurally partition model inputs $\mathbf{X}_{\text{input}}$ from evaluation ground truth $\mathbf{Y}_{\text{target}}$ such that natural sensor dropouts ($M_{\text{natural}} = 0$) are never treated as evaluation targets.
5. Create lightweight experimental artifacts under `data/interim/experiments/` without duplicating multi-gigabyte raw arrays.
6. Verify full alignment with Phase 4A Environmental Context Builder and SLM encoder.
7. Maintain 100% raw data and interim artifact immutability.

---

## 2. Work Executed & Engineering Architecture

### 2.1 Feature Normalization Engine (`src/dataset/normalization.py`)
- `FeatureNormalizer` class:
  - Fits normalization statistics strictly on training partition (294,528 station-hours).
  - Primary canonical contract: Standard z-score (`mode="z_score"`) across all 13 canonical channels.
  - Ablation contract 1: True, reversible `log1p` standardization for rainfall ($x' = (\log(1 + \max(0, x)) - \mu_{\log}) / \sigma_{\log}$, inverse $x = \max(0, \exp(x' \cdot \sigma_{\log} + \mu_{\log}) - 1)$) with non-negative guarantee.
  - Ablation contract 2: Dynamic 14-channel expansion via `transform_circular_wind()` ($\sin \theta, \cos \theta$) preserving 13-channel canonical disk format.
  - Ablation contracts 3 & 4: Min-max scaling $[0, 1]$ and robust scaling (median, IQR).
  - Exact round-trip denormalization verified with $< 10^{-4}$ numerical error.
  - Saved to `data/interim/experiments/normalization/normalization_stats.json`.

### 2.2 Deterministic Controlled Masking Engine (`src/dataset/missingness.py`)
- `ExperimentalMaskGenerator` class:
  - Methods: `create_random_mask()`, `create_block_mask()`, `create_station_outage_mask()`, `create_mask()`.
  - Contiguous temporal block trimming: candidate blocks are trimmed so that $N_{\text{masked}} \equiv N_{\text{target}}$, matching target rates within $\pm 0.07\%$ across all $7.26\text{M}$ eligible test cells.
  - Network outage modeling: S1 (1 station, $6.25\%$), S2 (2 stations, $12.50\%$), S4 (4 stations, $25.00\%$), and S_full (16 stations, $100.00\%$) with deterministic rotating station selection.
  - Enforces strict invariant: $M_{\text{artificial}} \le M_{\text{natural}}$.
  - Enforces partition: $M_{\text{observed}} = M_{\text{natural}} \odot (1 - M_{\text{artificial}})$, $M_{\text{target}} = M_{\text{natural}} \odot M_{\text{artificial}}$, $M_{\text{natural}} == M_{\text{observed}} + M_{\text{target}}$.
  - Input/target separation: `partition_inputs_and_targets()`.

### 2.3 Chronological Split Verification & Manifests (`src/dataset/split.py`)
- `ChronologicalSplitManager`:
  - Reconciled purge buffer metrics: 24h calendar day duration, 25.0h physical gap between split endpoints, and 752 excluded sliding windows ($47$ per station).
  - Generated `data/interim/experiments/splits/split_indices.parquet` ($3.68\text{ MB}$, $420,496$ rows) and `split_manifest.json`.

### 2.4 Benchmark Evaluation Mask Generation (`src/dataset/build_experimental_datasets.py`)
- Executed pipeline across all $62,224$ test partition windows ($7,261,392$ eligible criteria pollutant cells):
  - `mcar_10`: $731,326$ masked cells ($10.07\%$) [Dev: $+0.07\%$]
  - `mcar_30`: $2,174,824$ masked cells ($29.95\%$) [Dev: $-0.05\%$]
  - `mcar_50`: $3,627,817$ masked cells ($49.96\%$) [Dev: $-0.04\%$]
  - `mcar_70`: $5,085,285$ masked cells ($70.03\%$) [Dev: $+0.03\%$]
  - `block_10`: $731,326$ masked cells ($10.07\%$) [Dev: $+0.07\%$] (trimmed)
  - `block_30`: $2,174,824$ masked cells ($29.95\%$) [Dev: $-0.05\%$] (trimmed)
  - `block_50`: $3,627,817$ masked cells ($49.96\%$) [Dev: $-0.04\%$] (trimmed)
  - `block_70`: $5,085,285$ masked cells ($70.03\%$) [Dev: $+0.03\%$] (trimmed)
  - `station_outage_1`: $453,636$ masked cells ($6.25\%$) ($1/16$ stations)
  - `station_outage_2`: $907,691$ masked cells ($12.50\%$) ($2/16$ stations)
  - `station_outage_4`: $1,815,340$ masked cells ($25.00\%$) ($4/16$ stations)
  - `station_outage_full`: $7,261,392$ masked cells ($100.00\%$) ($16/16$ stations)
- Persisted to `data/interim/experiments/masks/test_benchmark_masks.parquet` ($7.39\text{ MB}$).
- Generated cryptographic reproducibility manifest (`reproducibility_manifest.json`).

### 2.5 Automated Quality Control Suite (`tests/test_experimental_dataset.py`)
- 15 comprehensive automated checks covering window uniqueness, temporal separation, channel ordering, natural NaN preservation, target masking invariants, train-only statistics, deterministic regeneration, raw data immutability, artifact-level test mask validation, log1p roundtrip fidelity, and circular wind decomposition.
- All 15 checks passed with zero errors. Combined suite: 17/17 passed in 8.70s.

---

## 3. Dataset Split & Masking Summary

| Split | Time Horizon | Windows | Eligible Test Cells | Masking Scenarios Pre-Computed |
| :--- | :--- | :---: | :---: | :--- |
| **Train** | `2019-01-01 00:00` to `2021-02-05 23:00` | $294,160$ ($69.96\%$) | — | Dynamic training masking during model training |
| **Buffer 1** | Purge day `2021-02-06` ($25.0\text{h}$ gap) | $752$ ($0.18\%$) | — | Purged ($47$ windows/station excluded) |
| **Val** | `2021-02-07 00:00` to `2021-07-20 23:00` | $62,608$ ($14.89\%$) | — | Validation tuning split |
| **Buffer 2** | Purge day `2021-07-21` ($25.0\text{h}$ gap) | $752$ ($0.18\%$) | — | Purged ($47$ windows/station excluded) |
| **Test** | `2021-07-22 00:00` to `2021-12-31 23:00` | $62,224$ ($14.80\%$) | $7,261,392$ | MCAR (4), Block (4), Station Outage (S1, S2, S4, S_full) |

---

## 4. Next Steps (Phase 4C)
- **Phase 4C: Model Architecture Scaffolding & Denoising Backbone Implementation**:
  - Implement spatial graph convolutional layers anchoring on verified Haversine distance matrix.
  - Implement temporal self-attention transformer blocks over 24-hour horizons.
  - Implement AdaLN conditioning block integrating $\mathbf{e}_k$ and $\mathbf{z}_C$.
  - Implement conditional forward diffusion process and composite loss objectives ($\mathcal{L}_{\text{diff}} + \mathcal{L}_{\text{nonneg}} + \mathcal{L}_{\text{photo}}$).
