project:
  name: air-pollution-imputation
  seed: 42

data:
  raw_dir: data/raw
  processed_dir: data/processed

dataset:
  window_size: 24

split:
  train: 0.70
  validation: 0.15
  test: 0.15

missingness:
  rates:
    - 0.10
    - 0.30
    - 0.50
    - 0.70
