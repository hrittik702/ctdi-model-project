# Research Checkpoint — 2026-09-17 (Phase 3)

## Session Status
STATUS: **`PHASE_3_WINDOW_CONSTRUCTION = COMPLETE`**  
*(Phase 3: 24-Hour Temporal Window Construction & Natural Missingness Representation successfully executed. Sliced the continuous 420,864-row Cartesian dataset into 420,496 chronological 24-hour sliding windows (26,281 windows per station across 16 stations) of shape (24, 13). Constructed binary natural-missingness masks M in {0, 1}^(24x13) preserving all 55,876 natural pollutant NaNs bit-for-bit without synthetic imputation or artificial corruption. Formulated leakage-safe chronological splits with 24-hour purge buffers. Serialized to data/interim/windows/ (62.73 MB windows, 3.23 MB masks, 3.88 MB metadata). All 8 validation suite checks passed. 100% cryptographic raw-data immutability verified on all 683 files in data/raw/. ML training NOT performed).*

---

## 1. Input Artifact
- **File**: `data/interim/aligned/aligned_hourly_station_data.parquet` ($6.42\text{ MB}$)
- **Rows / Columns**: $420,864\text{ rows} \times 19\text{ columns}$
- **Stations**: $16\text{ stations}$ (IDs: `66, 69, 70, 71, 72, 73, 74, 75, 76, 77, 78, 79, 80, 81, 82, 83`)
- **Hours per Station**: $26,304\text{ hours}$ (`2019-01-01 00:00:00` to `2021-12-31 23:00:00`)
- **Channels**: Canonical 13 channels in CTDI Table I order (rainfall at index 8 / channel #9).

---

## 2. Output Artifacts Generated
All stored in `data/interim/windows/`:
1. `24h_windows.parquet` ($62.73\text{ MB}$): $420,496\text{ rows}$, 13 `list<float32>` columns of length 24.
2. `missingness_masks.parquet` ($3.23\text{ MB}$): $420,496\text{ rows}$, 13 `list<uint8>` columns of length 24 ($1=\text{observed}, 0=\text{missing}$).
3. `window_metadata.parquet` ($3.88\text{ MB}$): $420,496\text{ rows} \times 14\text{ columns}$ (timestamps, calendar fields, missing counts, split labels).
4. `window_summary.json` ($1.4\text{ KB}$): Summary statistics.
5. `window_validation.json` ($2.8\text{ KB}$): Full automated validation audit results.

---

## 3. Window & Tensor Dimensions
- **Window Length ($L$)**: $24\text{ hours}$
- **Window Stride ($s$)**: $1\text{ hour}$
- **Windows per Station**: $26,304 - 24 + 1 = \mathbf{26,281}$
- **Total Stations**: $16$
- **Total Windows Generated**: $16 \times 26,281 = \mathbf{420,496}$
- **Single Window Tensor Dimension**: $\mathbf{X}^{(i)} \in \mathbb{R}^{24 \times 13}$
- **Single Mask Tensor Dimension**: $\mathbf{M}^{(i)} \in \{0, 1\}^{24 \times 13}$
- **Total Numerical Cells**: $420,496 \times 24 \times 13 = \mathbf{131,194,752}$

---

## 4. Missingness Counts & Conservation
- **Natural Pollutant Missingness Conserved**:
  - $\text{PM}_{2.5}$: $10,657\text{ NaNs}$
  - $\text{PM}_{10}$: $11,395\text{ NaNs}$
  - $\text{NO}_2$: $11,651\text{ NaNs}$
  - $\text{SO}_2$: $11,056\text{ NaNs}$
  - $\text{O}_3$: $11,117\text{ NaNs}$
  - **Total**: Exactly $\mathbf{55,876}\text{ NaNs}$ (100% bit-for-bit conservation).
- **Mask Agreement**: $(\mathbf{X} == \text{NaN}) \iff (\mathbf{M} == 0)$ verified across all $131,194,752$ cells.
- **Window Missingness Distribution**:
  - Windows with $\ge 1$ missing pollutant: $226,642$ ($53.90\%$)
  - Windows with $0$ missing pollutants: $193,854$ ($46.10\%$)
  - Mean missing pollutant cells per window: $3.19$ out of $120$ ($2.66\%$)
  - Max missing pollutant cells in a window: $120$ out of $120$ ($100.0\%$)

---

## 5. Leakage-Safe Chronological Split Distribution
- **Train Set (~70%)**: $294,160\text{ windows}$ ($18,385\text{ / station}$) — ends `2021-02-05 23:00:00`.
- **Buffer 1 (Train-Val Purge)**: $752\text{ windows}$ ($47\text{ / station}$) — `2021-02-06`.
- **Validation Set (~15%)**: $62,608\text{ windows}$ ($3,913\text{ / station}$) — `2021-02-07` to `2021-07-20`.
- **Buffer 2 (Val-Test Purge)**: $752\text{ windows}$ ($47\text{ / station}$) — `2021-07-21`.
- **Test Set (~15%)**: $62,224\text{ windows}$ ($3,889\text{ / station}$) — `2021-07-22` to `2021-12-31`.
- **Separation Buffer**: $25.0\text{ hours}$ between Train and Val, $25.0\text{ hours}$ between Val and Test.
- **Frame Leakage**: Exactly **0 timestamps shared** across splits.

---

## 6. Validation Results (8/8 Checks Passed)
1. `[PASS]` Input validation: 420,864 rows, 16 stations, 26,304 hours, canonical 13 channels.
2. `[PASS]` Natural missingness preservation: 55,876 pollutant NaNs conserved bit-for-bit.
3. `[PASS]` 24-hour window construction: 420,496 windows, 26,281 per station, zero station crossing.
4. `[PASS]` Mask construction: shape (24, 13), values strictly in {0, 1}, 100% agreement with NaNs.
5. `[PASS]` Temporal continuity: strictly 1-hour consecutive intervals for all 420,496 windows.
6. `[PASS]` Leakage validation: 24-hour purge buffers guarantee zero frame overlap across splits.
7. `[PASS]` Output validation: all 4 Parquet/JSON artifacts verified on disk and non-empty.
8. `[PASS]` Raw data integrity: 683 files audited via SHA-256 (0 modified, 0 added, 0 deleted, 0 missing).

---

## 7. Files Created and Modified

### Created:
- `src/preprocessing/build_windows.py`: Slices 24-hour sliding windows, generates natural masks, and exports Parquet.
- `src/preprocessing/validate_windows.py`: Automated 8-check validation suite.
- `data/interim/windows/24h_windows.parquet`: Window feature tensors ($62.73\text{ MB}$).
- `data/interim/windows/missingness_masks.parquet`: Binary natural masks ($3.23\text{ MB}$).
- `data/interim/windows/window_metadata.parquet`: Window metadata table ($3.88\text{ MB}$).
- `data/interim/windows/window_summary.json`: Summary statistics.
- `data/interim/windows/window_validation.json`: Validation audit record.
- `research/reports/Phase 3 - 24 Hour Window Construction.md`: Full 16-section research report.
- `research/checkpoints/2026-09-17_phase_3.md`: This checkpoint record.

### Modified:
- `research/research_status.md`: Updated with Phase 3 completion status.
- `research/research_timeline.md`: Appended Phase 3 milestone.
- `walkthrough.md`: Updated with Phase 3 validation evidence.

---

## 8. Machine Learning & Modeling Boundary
- **ML Training Occurred?**: **NO**.
- **Imputation Occurred?**: **NO**.
- **Synthetic Masking (10%–70%) Occurred?**: **NO**.
- **Feature Normalization Occurred?**: **NO**.
- **Phase Boundary Respected**: Preprocessing only. Execution STOPPED.
